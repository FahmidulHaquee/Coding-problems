function anagrams(word, words) {
  // Write a function that finds all the anagrams of a word
  // From a list of words

  // 0. Convert input word into standard list
  let standardisedWord = word.split("").sort().join("");

  // 1. Initialise output array
  let anagrams = [];

  // 2. Iterate through words list
  for (let element of words) {
    // 3. Compare
  }
}

let y = a.split("").sort().join("");
let z = b.split("").sort().join("");

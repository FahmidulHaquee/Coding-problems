function disemvowel(str) {
  // 1. Initialise vowels array
  const vowels = ["a", "e", "i", "o", "u"];

  // 2. Iterate through input string
  for (i = 0; i < str.length; i++) {
    if (vowels.includes(char)) {
      let index = str.indexOf(char);
    }
  }
  return str;
}
